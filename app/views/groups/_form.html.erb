<%= form_for(group) do |f| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :profile_img %>
    <%= f.text_field :profile_img %>
  </div>

  <div class="field">
    <%= f.label :background_img %>
    <%= f.text_field :background_img %>
  </div>

  <div class="field">
    <%= f.label :invite_code %>
    <%= f.text_field :invite_code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
